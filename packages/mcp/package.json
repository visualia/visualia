{
  "name": "@visualia/mcp",
  "version": "0.1.0",
  "description": "Agent access to a live visualia board: stdio MCP server, Vite ws relay plugin, and the in-page bridge. Engine-generic — the host app injects its verbs.",
  "type": "module",
  "exports": {
    "./bridge": "./bridge.ts",
    "./vite": "./vite.ts"
  },
  "bin": {
    "visualia-mcp": "./server.mjs"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ws": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
